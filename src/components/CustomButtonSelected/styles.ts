import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    actionItemButton: {
        padding: 6,
        borderRadius: 12,
        borderWidth: 1
    }
});

export default styles;