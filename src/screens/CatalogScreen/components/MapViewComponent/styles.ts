import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerMap: {
    marginTop: 30,
    width: 370,
    height: 370,
    borderRadius: 16,
  },
});

export default styles;
