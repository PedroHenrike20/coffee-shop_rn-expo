import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 30,
    },

    containerMap: {
        width: 370,
        height: 370,
        borderRadius: 16,
    }
})

export default styles;